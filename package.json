{
  "name": "otel-trace-analyzer",
  "version": "0.3.0",
  "description": "Analyze OpenTelemetry traces: critical path, slowest span by self-time, per-service time, N+1 detection, and error spans.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": { "otel-trace-analyzer": "dist/cli.js" },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": { "test": "vitest run", "build": "tsc -p tsconfig.json", "prepare": "npm run build" },
  "keywords": ["opentelemetry", "tracing", "critical-path", "performance", "observability", "cli"],
  "author": "Joshua John <josh202@gmail.com>",
  "license": "MIT",
  "devDependencies": { "vitest": "^4.0.0", "typescript": "^5.4.0", "@types/node": "^22.19.21" }
}
